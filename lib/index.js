'use strict';

// MODULES //



// INDEXOF //

/**
* FUNCTION: indexOf( obj, searchElement[, fromIndex] )
*	Returns the first index at which a given element can be found.
*
* @param {Array|String|Object} obj - array-like object
* @param {*} searchElement - element to find
* @param {Number} [fromIndex] - starting index (if negative, the start index is determined relative to last element)
* @returns {Number} first index or -1
*/
function indexOf( obj, val, i ) {

} // end FUNCTION indexOf()


// EXPORTS //

module.exports = indexOf;
